<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sombright.vizix.prodotaapis">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/dota2appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dota2appicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProMatchActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ProPlayerActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".MatchDetailsActivity" />
        <activity android:name=".MatchSummaryActivity"></activity>
    </application>

</manifest>